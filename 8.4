#Напишите программу, 
# которая считывает с клавиатуры два числа a и b,
# считает и выводит на консоль среднее арифметическое
# всех чисел из отрезка [a; b], которые кратны числу c.
# Подсказка: (a и b  являются промежутком, а c для проверки кратности).


startNumm = int(input('Введите начало отрезка: '))
stopNumm = int(input('Введите конец отрезка: '))
divider = int(input('Введите делитель: '))
check = 0
sum_elem = 0
for number in range (startNumm, stopNumm + 1):
  if number % divider == 0:
    sum_elem += number
    check += 1
print('Среднее арифметическое чисел кратных', divider, 'равна', sum_elem / check)
